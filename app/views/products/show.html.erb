<p id="notice"><%= notice %></p>
<%= link_to '<-Back to Products', products_path %>
<div>
<p>
  <strong>Name:</strong>
  <%= @product.name %>
</p>

<%= image_tag(@product.image_url(:default)) %>

<p>
  <strong>Description:</strong>
  <%= @product.description %>
</p>

<p>
  <strong>Release date:</strong>
  <%= @product.release_date %>
</p>

<p>
  <strong>Price: £</strong>
  <%= @product.price.to_i %>
</p>
<%= link_to 'Edit', edit_product_path(@product) %>
</div>
<%= button_to 'Delete', @product, :method => "delete", data: { confirm: "Are you sure?" }, class: 'product-page-button' %>
<%= button_to 'Add to basket', basket_items_path(product_id: @product), class: 'product-page-button' %>
